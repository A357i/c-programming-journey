/*
 * REFRESH 03 - Combined practice challenge
 *
 * Use only variables, input, conditions, strings, for, while, do-while,
 * nested loops, break, and the arithmetic operators already practiced.
 */

#include <stdio.h>
#include <string.h>

int main(void)
{
    /*
     * Challenge 1 - Number analyzer
     * Ask for a positive integer. Print its digits one by one, count its
     * digits, calculate the sum of its digits, and report whether it is even.
     * First trace the program for 482 on paper.
     *
     * Challenge 2 - Repeated calculator
     * Show a menu for addition, subtraction, multiplication, and division.
     * Read the user's choice, request the needed values, print the result,
     * and repeat until the user chooses to stop. Predict the exact order of
     * prompts for two operations followed by stop.
     *
     * Challenge 3 - Pattern builder
     * Ask for a positive size and print a square, an increasing triangle,
     * and a decreasing triangle. For each pattern, state what the outer and
     * inner loops control before writing them.
     *
     * Challenge 4 - Guessing practice
     * Store a fixed secret number in a variable. Keep asking for guesses
     * until the user is correct. Report whether each guess is too high or
     * too low, and count the attempts. Decide which loop structure fits.
     *
     * Challenge 5 - Input report
     * Ask for a name and then repeatedly ask for scores until a sentinel
     * value is entered. Print the name, number of scores, total, and average.
     * Before coding, decide how the sentinel affects the total and count.
     *
     * Final review for every challenge:
     * - Can you predict the first two iterations?
     * - Can the loop execute zero times, or must it execute once?
     * - What changes after each iteration?
     * - What input or condition guarantees that it stops?
     */

    return 0;
}