/*
 * REFRESH 02 - Input validation and loop reasoning
 *
 * Complete one exercise at a time. Add your own comments explaining why
 * the condition is true or false on each important iteration.
 */

#include <stdio.h>
#include <string.h>

int main(void)
{
    /*
     * 1. Ask for a number between 1 and 10. Repeat while the input is
     *    outside that range. Test the boundary values 1, 10, 0, and 11.
     *
     * 2. Ask for a word until it matches either "yes" or "no". Decide how
     *    you will handle uppercase input using only the string techniques
     *    already used in the exercises.
     *
     * 3. Ask for numbers until the user enters 0. Add only positive numbers.
     *    Predict the final sum for: 4, -2, 7, 0.
     *
     * 4. Ask for a number and print its divisors. What does remainder 0
     *    tell you? How many times should the loop test a possible divisor?
     *
     * 5. Ask for a number and determine whether it is prime. Trace the
     *    divisor checks for 2, 7, and 12 before running.
     *
     * 6. Build a menu that repeats until the user chooses an exit option.
     *    For an invalid choice, show an error and display the menu again.
     *    Explain why do-while is suitable here.
     *
     * 7. Read several numbers and track the smallest and largest value.
     *    Decide what the initial values should be before writing the loop.
     *
     * 8. For each exercise, answer:
     *    - What initializes the loop?
     *    - What makes progress toward the ending condition?
     *    - What exact input ends the loop?
     */

    return 0;
}