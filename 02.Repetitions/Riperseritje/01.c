/*
 * REFRESH 01 - Foundations + Repetitions
 *
 * Work through these in order. Uncomment or write only one task at a time.
 * Do not look for a new concept outside the Foundations and Repetitions folders.
 * Before running each task, write down the expected output and variable values.
 */

#include <stdio.h>
#include <string.h>

int main(void)
{
	/*
	 * PART A - Read and predict
	 *
	 * 1. For each expression, predict its value before running it:
	 *        7 / 2
	 *        7 % 2
	 *        8 % 2
	 *        9 % 2
	 *        2 + 3 * 4
	 *        (2 + 3) * 4
	 *
	 * 2. Predict the output and final value of total:
	 *        int total = 0;
	 *        for (int i = 1; i <= 4; i++)
	 *        {
	 *            total += i;
	 *        }
	 *
	 * 3. Predict the output of a nested loop with an outer counter from
	 *    1 to 3 and an inner counter from 1 to 2. Explain the order.
	 *
	 * 4. Explain in your own words what changes when a while loop becomes
	 *    a do-while loop.
	 */

	/*
	 * PART B - Basic input
	 *
	 * 5. Ask for a name and an age. Read the name as a string and the age
	 *    as an integer. Print both values in one sentence.
	 *    Check your understanding:
	 *    - Which input function fits the name?
	 *    - Which input function fits the age?
	 *    - Where is the string terminator?
	 *
	 * 6. Ask for two integers. Print their sum, difference, product,
	 *    quotient, and remainder. Predict the result before testing with
	 *    17 and 5. What happens if the second number is zero?
	 *
	 * 7. Ask for a temperature and print the converted value using the
	 *    arithmetic operators you already learned. State the formula first.
	 *
	 * 8. Ask for a character and print whether it matches a chosen character
	 *    using the comparison ideas from the foundations exercises.
	 */

	/*
	 * PART C - Conditions and repetition
	 *
	 * 9. Ask for an integer and report whether it is positive, negative,
	 *    or zero. Then report whether it is even or odd.
	 *    Before coding, predict the result for -4, 0, and 7.
	 *
	 * 10. Print the numbers from 1 through 20. Beside each number, identify
	 *     whether it is even or odd. Decide whether for or while is clearer
	 *     and explain why.
	 *
	 * 11. Ask for a name repeatedly until the user enters a non-empty name.
	 *     Predict what the string contains when the user presses Enter only.
	 *
	 * 12. Ask for a password repeatedly until it equals the expected text.
	 *     Explain why string comparison cannot use the same operator as
	 *     integer comparison.
	 *
	 * 13. Create a do-while menu with three choices. It must appear at least
	 *     once and repeat after an invalid choice. Decide what ends the loop.
	 */

	/*
	 * PART D - Small programs
	 *
	 * 14. Print a multiplication table for a number entered by the user.
	 *
	 * 15. Read five integers and calculate their sum. Before running,
	 *     trace the accumulator after every iteration.
	 *
	 * 16. Read a positive integer and calculate its factorial. What should
	 *     the starting value of the product be, and why?
	 *
	 * 17. Print a rectangle of stars. Ask for the number of rows and columns.
	 *     Identify which loop controls rows and which controls columns.
	 *
	 * 18. Print numbers from 1 through 30 with the FizzBuzz rules from the
	 *     repetition exercises. Which condition must be checked first?
	 */

	return 0;
}
