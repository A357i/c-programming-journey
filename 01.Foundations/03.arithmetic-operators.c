#include <stdio.h>

int main(){

    // arithmetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment 
    // -- decrement

    int x = 5;
    int y = 8;

    // int z = x + y;
    // int z = x - y;
    // int z = x * y;
    // float z = x + (float) y;
    // float z = x % y;
    
    x++; // x = x + 1
    y--; // y = y - 1

    printf("%d", y);

    return 0;
}