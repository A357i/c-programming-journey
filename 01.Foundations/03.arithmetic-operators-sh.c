#include <stdio.h>

int main(){

    // Operatorët

    // + (Mbledhja)
    // - (Zbritja)
    // * (Shumzimi)
    // / (Pjestimi)
    // % (Mbetja - vetem per numra te plot 'int')
    // ++ Operatori i rritjes (shton 1)
    // -- Operatori i zvogëlimit (heq 1)

    int x = 5;
    int y = 8;

    // int z = x + y;
    // int z = x - y;
    // int z = x * y;
    // float z = x + (float) y;
    // float z = x % y;
    
    x++; // x = x + 1
    y--; // y = y - 1

    printf("%d", y);

    return 0;
}